<view class="nav-bar {{isDarkMode ? 'dark' : ''}}" style="padding-top: {{statusBarHeight}}px;">
  <view class="nav-bar__left" wx:if="{{showBack}}" bindtap="onBack">
    <text class="nav-bar__back">‹</text>
  </view>
  <view class="nav-bar__title">
    <text>{{title}}</text>
  </view>
  <view class="nav-bar__right">
    <slot name="right"></slot>
  </view>
</view>
<view style="height: {{statusBarHeight + 44}}px;"></view>
